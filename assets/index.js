// node package inquirer
const inquirer = require("inquirer");

// importing node package system file
const fs = require("fs");

// Getting the classes from directory
const { Rectangle, Square, Triangle } = require(".spec/figures");

